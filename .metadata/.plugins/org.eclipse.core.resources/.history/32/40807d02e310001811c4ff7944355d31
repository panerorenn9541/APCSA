//© A+ Computer Science  -  www.apluscompsci.com
//Name -
//Date -
//Class -
//Lab  -

import static java.lang.System.*;

public class LoopStats
{
	private int start, stop;

	public LoopStats()
	{
		start = 0;
		stop = 0;
		setNums(0, 0);
	}

	public LoopStats(int beg, int end)
	{
		setNums(beg, end);
	}

	public void setNums(int beg, int end)
	{
		start = beg;
		stop = end;
	}

	public int getEvenCount()
	{
		int evenCount=0;
		for(start++; start<stop+1; start = start+1)
		{
			if (start%2 == 0)
			{
				evenCount = evenCount+1;
			}
		}


		return evenCount;
	}

	public int getOddCount()
	{
		int oddCount=start;
		for(start++; start<stop+1; start = start+1)
		{
			oddCount = oddCount+1;
		}

		return oddCount;
	}
	
	public int getTotal()
	{
		int total=start;
		for(start++; start<stop+1; start = start+1)
		{
			total = total+start;
		}

		return total;
	}
	
	public String toString()
	{
		return start + " " + stop + "\n" + "total: " + getTotal() + "\neven count: " + getEvenCount() + "\nodd count: " + getOddCount() + "\n\n";
	}
}